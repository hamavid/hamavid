

window.onload = function () {
/*
// functions for playing demo gif
  // set variables
	var playstop = document.getElementById('playstop');
	var playstopicon = playstop.getElementsByTagName('i')[0];
	var demoimg = document.getElementById('demo').getElementsByTagName('img')[0];
  
  // play or stop when the icon is clicked by replacing stand-in png with animated gif and vice versa
  // also switch out the icons
	playstop.onclick = playstopper;
	function playstopper() {
		var currentimg = demoimg.src.substring(demoimg.src.indexOf('robot/')+6);
		console.log(currentimg);
		if (currentimg == 'start.png') {
			console.log('yep');
			demoimg.src=demoimg.src.replace(currentimg,'speedyopenclose.gif');
			playstopicon.classList.remove('fa-play-circle');
			playstopicon.classList.add('fa-stop-circle');
		}
		else {
			demoimg.src=demoimg.src.replace(currentimg,'start.png');
			playstopicon.classList.remove('fa-stop-circle');
			playstopicon.classList.add('fa-play-circle');
		}
	}*/

}